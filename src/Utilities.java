public class Utilities {

    // Strings

    public static String stringRepeat(String str, int iterations) {
        String outStr = "";
        for (int a = 0; a < iterations; a++) outStr += str;
        return outStr;
    }
}
